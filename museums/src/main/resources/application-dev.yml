server:
  port: 8080

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/nationa_museum?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver


mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.king.springboot.pojo

#showSql
logging:
  level:
    com:
      example:
        mapper : debug

thymeleaf:
  cache: false

#  spring:
#    mvc:
#      static-path-pattern=/static/**:

